# Example environment file (for developers)
# Copy this to .env and customize as needed
# Never commit .env to version control

# Development settings
NODE_ENV=production
DEBUG=false

# App settings  
APP_NAME=Alfred Lens
DEFAULT_SHORTCUT=Command+Shift+S